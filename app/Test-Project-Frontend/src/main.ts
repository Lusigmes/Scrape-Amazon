import { SearchControl } from "./api/control/SearchControl";


// Cria uma nova instância do SearchControll, que gerencia a lógica de busca
// Creates a new instance of SearchControll, which manages the search logic
new SearchControl("keyword","results","searchBtn");